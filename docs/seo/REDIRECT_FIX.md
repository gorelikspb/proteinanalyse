# Исправление проблемы с редиректами

## 🔍 Проблема

Google Search Console показывает ошибку для `/reverse-complement`:
- **"Redirect error"** - редирект не работает правильно
- **"Duplicate without user-selected canonical"** - Google видит дубликат

## ✅ Решение

**Удалены редиректы для URL без `.html` расширения.**

### Почему это правильно:

1. **Canonical теги уже установлены** - каждая страница указывает на свою `.html` версию
2. **Google будет индексировать только `.html` версии** - это правильно для SEO
3. **Нет ошибок редиректов** - Google не будет пытаться индексировать несуществующие URL

### Что осталось в `_redirects`:

- `/index.html` → `/` (301) - это правильно и нужно

---

## 📋 Что делать дальше

### 1. Задеплой изменения
```bash
git add public/_redirects
git commit -m "Fix redirects: remove .html redirects to avoid GSC errors"
git push
```

### 2. В Google Search Console

**Для `/reverse-complement` (без .html):**
- URL Inspection → `https://seqanalysis.org/reverse-complement`
- Google покажет что страница не существует (это нормально)
- Google будет индексировать только `/reverse-complement.html` (с canonical)

**Запроси переиндексацию для правильного URL:**
- URL Inspection → `https://seqanalysis.org/reverse-complement.html`
- Request Indexing ✅

### 3. Проверь через 1-2 недели

- Проблема "Redirect error" должна исчезнуть
- Проблема "Duplicate without canonical" должна исчезнуть
- Google будет индексировать только `.html` версии

---

## 🎯 Итог

**Теперь:**
- ✅ Google индексирует только `.html` версии
- ✅ Canonical теги указывают на правильные URL
- ✅ Нет ошибок редиректов
- ✅ Чистая структура URL для SEO

**URL без `.html` просто не существуют** - это правильно и избегает проблем с дубликатами.

