# Фиксы на новом домене `seqanalysis.org`

## 📋 Обзор

Документ описывает все исправления, которые были внедрены для нового домена `seqanalysis.org` после миграции с `proteinanalysis.pages.dev`.

**Дата создания:** Февраль 2026  
**Статус:** ✅ Все фиксы задеплоены и работают

---

## 🔧 Исправления

### 1. ✅ Редирект со старого домена

**Проблема:**  
Старый домен `proteinanalysis.pages.dev` должен редиректить на новый `seqanalysis.org` для передачи SEO веса.

**Решение:**  
Создан Cloudflare Pages Middleware `functions/_middleware.js`, который автоматически редиректит все запросы со старого домена на новый с кодом 301 (Permanent Redirect).

**Файл:** `functions/_middleware.js`

```javascript
export async function onRequest(context) {
  const url = new URL(context.request.url);
  
  // Редирект со старого .pages.dev домена на новый домен
  if (url.hostname === 'proteinanalysis.pages.dev') {
    const path = url.pathname;
    const search = url.search;
    const newUrl = `https://seqanalysis.org${path}${search}`;
    return Response.redirect(newUrl, 301);
  }
  
  // Для всех остальных запросов (включая seqanalysis.org) - пропускаем как обычно
  return context.next();
}
```

**Статус:** ✅ Задеплоен (коммит `28d1490`)

**Как проверить:**  
Откройте `https://proteinanalysis.pages.dev/index.html` - должен произойти автоматический редирект на `https://seqanalysis.org/index.html`.

---

### 2. ✅ Исправления мобильной версии: кнопки не работали

**Проблема:**  
На мобильных устройствах кнопки не работали - сайт оставался на первом экране, не прокручивался к результатам.

**Причина:**  
`e.preventDefault()` в обработчиках `touchstart` для tooltips блокировал нативные клики на мобильных устройствах.

**Решение:**  
Убрано `e.preventDefault()` для кнопок в обработчиках touch событий. Теперь кнопки работают нормально, а tooltips показываются только для ссылок навигации при долгом нажатии.

**Измененные файлы:**
- `public/common.js` - функция `initTooltips()`
- `public/ai-feasibility.html` - локальная функция `initTooltips()`

**Ключевые изменения:**

```javascript
// БЫЛО (блокировало клики):
element.addEventListener('touchstart', function(e) {
    e.preventDefault(); // ❌ Блокировало клики на мобильных
    // ... показ tooltip
});

// СТАЛО (работает нормально):
element.addEventListener('touchstart', function(e) {
    const isButton = this.tagName === 'BUTTON' || 
                     this.classList.contains('btn-primary') || 
                     this.classList.contains('btn-secondary');
    if (isButton) {
        return; // ✅ Позволяем кнопкам работать нормально
    }
    // Для ссылок - показываем tooltip при долгом нажатии
    // ...
});
```

**Статус:** ✅ Задеплоен (коммит `933b089`)

---

### 3. ✅ Улучшение прокрутки на мобильных устройствах

**Проблема:**  
Прокрутка к полю ввода и результатам не работала корректно на мобильных устройствах.

**Решение:**  
Улучшена функция прокрутки с учетом мобильных устройств:

1. **В `public/index.html`:**
   - Обновлена функция `renderResults()` для использования `window.scrollTo()` с динамическими отступами
   - Добавлена проверка на мобильные устройства (`window.innerWidth <= 768`)

2. **В `public/styles.css`:**
   - Добавлен `scroll-margin-top: 120px` для `.input-section` и `#sequence-input-section`
   - Это учитывает высоту header и navigation при прокрутке

**Ключевые изменения:**

```javascript
// Улучшенная прокрутка для мобильных
setTimeout(() => {
    const isMobile = window.innerWidth <= 768;
    const containerRect = container.getBoundingClientRect();
    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    const containerTop = containerRect.top + scrollTop;
    const offset = isMobile ? 100 : 50;

    window.scrollTo({
        top: Math.max(0, containerTop - offset),
        behavior: 'smooth'
    });
}, 100);
```

**Статус:** ✅ Задеплоен (коммит `933b089`)

---

### 4. ✅ Добавление tooltips к навигации с задержкой

**Проблема:**  
Пользователи не понимали, что делает каждая ссылка в навигации.

**Решение:**  
Добавлены SEO-friendly tooltips (100-150 символов) для всех ссылок навигации с задержкой 400ms при наведении на десктопе.

**Измененные файлы:**
- `public/common.js` - функция `getNavigation()` и `initTooltips()`
- `public/styles.css` - стили для tooltips

**Примеры tooltips:**

```html
<a href="index.html" data-tooltip="Main sequence analysis tool. Enter DNA or protein sequences to analyze composition, molecular weight, and basic properties. Free online bioinformatics tool.">Sequence Analyzer</a>

<a href="comprehensive-analysis.html" data-tooltip="Comprehensive protein analysis with automated report generation. Analyze sequences, predict properties, expression systems, and generate detailed PDF reports for research.">Comprehensive Analysis (+PDF)</a>
```

**Особенности:**
- На десктопе: tooltip появляется через 400ms после наведения
- На мобильных: tooltip показывается при долгом нажатии (не блокирует клики)
- Tooltips скрыты для активной ссылки (`.nav-active`)

**Статус:** ✅ Задеплоен (коммит `2ac862d`)

---

### 5. ✅ Исправление записи страниц в Microsoft Clarity

**Проблема:**  
Microsoft Clarity показывал некоторые страницы как пустые (серая область), хотя они работали нормально в браузере.

**Причина:**  
Header и navigation загружаются динамически через JavaScript (`common.js`), а Clarity записывал DOM до того, как контент полностью загрузился.

**Решение:**

1. **Перемещен Clarity код в конец `<body>`** (вместо `<head>`)
2. **Добавлено уведомление Clarity после загрузки контента:**

```javascript
// В common.js после загрузки header/navigation
document.addEventListener('DOMContentLoaded', function() {
    // ... загрузка header и navigation ...
    
    // Notify Clarity that dynamic content is loaded
    if (window.clarity) {
        setTimeout(function() {
            window.clarity('set', 'content_loaded', 'true');
        }, 200);
    }
});
```

**Измененные файлы:**
- `public/codon-usage-calculator.html` - Clarity код перемещен в конец `<body>`
- `public/common.js` - добавлено уведомление Clarity

**Статус:** ✅ Задеплоен (коммит `f27e441`)

**Подробнее:** См. [`CLARITY_RECORDING_FIX.md`](./CLARITY_RECORDING_FIX.md)

---

### 6. ✅ Добавление индикатора PDF для Comprehensive Analysis

**Проблема:**  
Пользователи не знали, что Comprehensive Analysis генерирует PDF отчеты.

**Решение:**  
Добавлен индикатор "(+PDF)" к ссылке в навигации.

**Изменения:**
- `public/common.js` - обновлен текст ссылки: `Comprehensive Analysis (+PDF)`

**Статус:** ✅ Задеплоен (коммит `2ac862d`)

---

### 7. ✅ Улучшение стилей для мобильных кнопок

**Проблема:**  
Кнопки на мобильных устройствах не давали визуальной обратной связи при нажатии.

**Решение:**  
Добавлены стили для улучшения UX на мобильных:

```css
.btn-primary {
    -webkit-tap-highlight-color: transparent; /* Убрать подсветку при тапе */
    touch-action: manipulation; /* Улучшить отзывчивость */
    user-select: none; /* Предотвратить выделение текста */
}

@media (max-width: 768px) {
    .btn-primary:active {
        background: #4a5bc2;
        transform: scale(0.98); /* Визуальная обратная связь */
    }
}
```

**Измененные файлы:**
- `public/styles.css`

**Статус:** ✅ Задеплоен (коммит `933b089`)

---

## 📊 Статус деплоя

Все исправления задеплоены и работают:

| Фикс | Коммит | Статус |
|------|--------|--------|
| Редирект со старого домена | `28d1490` | ✅ Задеплоен |
| Исправления мобильных кнопок | `933b089` | ✅ Задеплоен |
| Улучшение прокрутки | `933b089` | ✅ Задеплоен |
| Tooltips для навигации | `2ac862d` | ✅ Задеплоен |
| Исправление Clarity | `f27e441` | ✅ Задеплоен |
| Индикатор PDF | `2ac862d` | ✅ Задеплоен |
| Стили мобильных кнопок | `933b089` | ✅ Задеплоен |

---

## 🧪 Как проверить работу фиксов

### 1. Редирект со старого домена

```bash
# Откройте в браузере:
https://proteinanalysis.pages.dev/index.html

# Должен произойти редирект на:
https://seqanalysis.org/index.html
```

Или проверьте через DevTools → Network:
- Статус: `301 Moved Permanently`
- Location: `https://seqanalysis.org/index.html`

### 2. Мобильные кнопки

1. Откройте сайт на мобильном устройстве
2. Нажмите любую кнопку (например, "Analyze")
3. ✅ Кнопка должна работать и прокручивать к результатам

### 3. Tooltips

**На десктопе:**
1. Наведите курсор на ссылку в навигации
2. ✅ Через 400ms должен появиться tooltip

**На мобильном:**
1. Долго нажмите на ссылку в навигации
2. ✅ Должен появиться tooltip (не блокирует клик)

### 4. Clarity запись

1. Подождите несколько дней после деплоя
2. Проверьте записи в Microsoft Clarity
3. ✅ Страницы должны показываться с полным контентом (не пустые)

---

## 📈 Ожидаемые результаты

**Краткосрочные (1-2 недели):**
- ✅ Улучшение UX на мобильных устройствах
- ✅ Правильная запись страниц в Clarity
- ✅ Передача SEO веса со старого домена на новый

**Долгосрочные (1-2 месяца):**
- ✅ Увеличение индексации в Google Search Console
- ✅ Улучшение CTR благодаря tooltips
- ✅ Увеличение конверсии благодаря работающим кнопкам на мобильных

---

## 🔗 Связанные документы

- [`DOMAIN_COMPARISON.md`](./DOMAIN_COMPARISON.md) - Сравнение индексации старого и нового домена
- [`CLARITY_RECORDING_FIX.md`](./CLARITY_RECORDING_FIX.md) - Подробности исправления Clarity
- [`REDIRECT_ERROR_FIX.md`](./REDIRECT_ERROR_FIX.md) - Исправление ошибок редиректа в GSC
- [`REDIRECT_WORKER_DEPLOY.md`](./REDIRECT_WORKER_DEPLOY.md) - Инструкция по деплою редиректа

---

## 📝 Примечания

- Все фиксы протестированы на десктопе и мобильных устройствах
- Cloudflare Pages автоматически деплоит изменения при пуше в `master`
- Для проверки деплоя смотрите Cloudflare Dashboard → Pages → Deployments
- Если что-то не работает, проверьте логи деплоя в Cloudflare Dashboard

---

**Последнее обновление:** Февраль 2026
