# Исправление проблемы с записью страниц в Microsoft Clarity

## 🔍 Проблема

Microsoft Clarity показывает страницы как будто они не загрузились (серая пустая область), хотя на самом деле страницы работают нормально.

**Примеры:**
- Страница `codon-usage-calculator.html` показывается как пустая в Clarity
- Страница `sequence-translation.html` показывается правильно

## 🔍 Причина

**Проблема:** Header и navigation загружаются динамически через JavaScript (`common.js`), а Clarity записывает DOM до того, как контент полностью загружен.

**Когда это происходит:**
1. Clarity код загружается в `<head>` (раньше, чем контент заполняется)
2. JavaScript заполняет header и navigation при `DOMContentLoaded`
3. Если вкладка скрыта сразу после загрузки (00:01), Clarity записывает пустые div'ы
4. В результате Clarity показывает страницу как будто она не загрузилась

## ✅ Решение

### 1. Переместить Clarity код в конец `<body>`

**Было (в `<head>`):**
```html
<head>
    ...
    <!-- Microsoft Clarity -->
    <script type="text/javascript">
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "u5y6bxpy0v");
    </script>
</head>
```

**Стало (в конце `<body>`):**
```html
<body>
    ...
    <!-- Microsoft Clarity - Loaded at end to ensure content is rendered -->
    <script type="text/javascript">
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "u5y6bxpy0v");
        
        // Notify Clarity that page content is fully loaded
        window.addEventListener('load', function() {
            if (window.clarity) {
                setTimeout(function() {
                    window.clarity('set', 'page_loaded', 'true');
                }, 100);
            }
        });
    </script>
</body>
```

### 2. Добавить уведомление Clarity после загрузки контента

В `common.js` добавлено уведомление Clarity после того, как header и navigation загружены:

```javascript
document.addEventListener('DOMContentLoaded', function() {
    // ... загрузка header и navigation ...
    
    // Notify Clarity that dynamic content is loaded
    if (window.clarity) {
        setTimeout(function() {
            window.clarity('set', 'content_loaded', 'true');
        }, 200);
    }
});
```

## 📋 Что было исправлено

1. **`codon-usage-calculator.html`:**
   - Clarity код перемещен в конец `<body>`
   - Добавлено уведомление Clarity после загрузки страницы

2. **`common.js`:**
   - Добавлено уведомление Clarity после загрузки динамического контента
   - Работает для всех страниц автоматически

## 🎯 Ожидаемый результат

После исправления:
- ✅ Clarity будет записывать страницы с полным контентом
- ✅ Header и navigation будут видны в записях Clarity
- ✅ Страницы не будут показываться как пустые

## ⚠️ Важно

**Для других страниц:**
- Если Clarity код находится в `<head>`, перемести его в конец `<body>`
- Или используй автоматическое уведомление из `common.js` (уже добавлено)

**Проверка:**
1. Открой страницу в браузере
2. Проверь, что header и navigation загружаются
3. Подожди несколько дней
4. Проверь запись в Clarity - страница должна показываться правильно

---

## 📝 Примечания

- Clarity записывает DOM в момент загрузки страницы
- Если контент загружается динамически через JavaScript, Clarity может не успеть его записать
- Перемещение Clarity кода в конец body и уведомление после загрузки решает эту проблему
- Небольшая задержка (100-200ms) нужна, чтобы убедиться, что весь контент загружен
