# Настройка редиректа через Cloudflare Page Rules

## 🎯 Цель

Настроить автоматический редирект 301 с `proteinanalysis.pages.dev` на `seqanalysis.org` через Cloudflare Page Rules.

## ✅ Пошаговая инструкция

### Шаг 1: Войди в Cloudflare Dashboard

1. Открой https://dash.cloudflare.com
2. Выбери аккаунт, где находится проект `proteinanalyse`

### Шаг 2: Открой Page Rules

1. В левом меню выбери **Rules** → **Page Rules**
2. Или перейди напрямую: https://dash.cloudflare.com → выбери аккаунт → **Rules** → **Page Rules**

### Шаг 3: Создай новое правило

1. Нажми кнопку **Create Page Rule** (или **+ Create rule**)
2. В поле **URL pattern** введи: `proteinanalysis.pages.dev/*`
   - ⚠️ Важно: используй `*` в конце, чтобы правило применялось ко всем путям
   - Примеры: `/`, `/index.html`, `/dna-gc-calculator.html` и т.д.

### Шаг 4: Настрой редирект

1. В разделе **Then the settings are:** нажми **+ Add a Setting**
2. Выбери **Forwarding URL**
3. Выбери **301 - Permanent Redirect**
4. В поле **Destination URL** введи: `https://seqanalysis.org/$1`
   - ⚠️ Важно: `$1` сохраняет путь после домена
   - Например: `proteinanalysis.pages.dev/index.html` → `seqanalysis.org/index.html`
   - `proteinanalysis.pages.dev/dna-gc-calculator.html` → `seqanalysis.org/dna-gc-calculator.html`

### Шаг 5: Сохрани правило

1. Нажми **Save and Deploy**
2. Подожди несколько секунд - правило должно активироваться

### Шаг 6: Проверь работу

1. Открой в браузере: `https://proteinanalysis.pages.dev/`
2. Должен произойти автоматический редирект на `https://seqanalysis.org/`
3. Проверь в DevTools → Network:
   - Статус должен быть `301 Moved Permanently`
   - Header `Location` должен быть `https://seqanalysis.org/`

## 📋 Пример настройки

**URL pattern:**
```
proteinanalysis.pages.dev/*
```

**Setting:**
- **Forwarding URL** → **301 - Permanent Redirect**
- **Destination URL:** `https://seqanalysis.org/$1`

## ✅ Проверка через curl

```bash
curl -I https://proteinanalysis.pages.dev/
```

**Ожидаемый результат:**
```
HTTP/2 301
location: https://seqanalysis.org/
```

## ⚠️ Важные моменты

1. **Порядок правил важен:**
   - Page Rules применяются сверху вниз
   - Убедись, что правило для `proteinanalysis.pages.dev/*` не перекрывается другими правилами

2. **Лимиты:**
   - Бесплатный план: 3 Page Rules
   - Платные планы: больше правил

3. **Время активации:**
   - Правило активируется сразу после сохранения
   - Может потребоваться несколько секунд для распространения

4. **Проверка:**
   - После настройки проверь несколько URL:
     - `https://proteinanalysis.pages.dev/`
     - `https://proteinanalysis.pages.dev/index.html`
     - `https://proteinanalysis.pages.dev/dna-gc-calculator.html`

## 🔍 Устранение проблем

### Редирект не работает

1. Проверь, что URL pattern правильный: `proteinanalysis.pages.dev/*`
2. Проверь, что правило активно (не отключено)
3. Очисти кэш браузера и попробуй снова
4. Проверь через curl или другой браузер

### Редирект работает, но теряется путь

1. Убедись, что в Destination URL используется `$1` (не `$2` или другое)
2. Проверь формат: `https://seqanalysis.org/$1`

### Правило не сохраняется

1. Проверь лимиты Page Rules для твоего плана
2. Убедись, что у тебя есть права на изменение правил
3. Попробуй удалить старое правило и создать новое

## 📝 Альтернатива: Bulk Redirects (для платных планов)

Если у тебя платный план Cloudflare, можно использовать **Bulk Redirects**:

1. **Rules** → **Redirect Rules** → **Create rule**
2. **Source URL:** `proteinanalysis.pages.dev/*`
3. **Destination URL:** `https://seqanalysis.org/$1`
4. **Status code:** `301`

Это более современный способ, но требует платного плана.

---

**Последнее обновление:** Февраль 2026
