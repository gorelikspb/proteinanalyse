# ⚠️ СРОЧНО: Настройка редиректа со старого домена

## 🔴 Критическая ситуация

**Проблема:**
- Старый домен `proteinanalysis.pages.dev` получает **1.64k impressions** (90% трафика)
- Новый домен `seqanalysis.org` получает только **174 impressions** (10% трафика)
- **Теряешь 90% потенциального трафика!**

**Данные:**
- Старый домен: 1.64k impressions, позиция 17.9, 9 кликов
- Новый домен: 174 impressions, позиция 38.7, 0 кликов
- **Разница: в 9 раз больше трафика на старом домене**

---

## ✅ Решение: Редирект 301 через Cloudflare Workers

**⚠️ ВАЖНО:** Page Rules работают только для купленных доменов (`.org`, `.com` и т.д.).  
Для `.pages.dev` доменов нужно использовать **Cloudflare Workers**.

### Способ 1: Cloudflare Workers (РЕКОМЕНДУЕТСЯ для .pages.dev) ⭐⭐⭐

**⚠️ Важно:** Page Rules работают только для купленных доменов. Для `.pages.dev` доменов нужно использовать Workers.

**Шаги:**

1. **Открой Cloudflare Dashboard:**
   - https://dash.cloudflare.com/
   - Выбери аккаунт

2. **Создай Worker:**
   - Workers & Pages → Create → Worker
   - Имя: `redirect-old-domain` (или любое другое)

3. **Вставь код Worker:**
```javascript
export default {
  async fetch(request) {
    const url = new URL(request.url);
    
    // Редирект со старого .pages.dev домена на новый домен
    if (url.hostname === 'proteinanalysis.pages.dev') {
      // Сохраняем путь и query параметры
      const path = url.pathname;
      const search = url.search;
      
      // Редирект на новый домен с сохранением пути
      const newUrl = `https://seqanalysis.org${path}${search}`;
      
      return Response.redirect(newUrl, 301);
    }
    
    // Для всех остальных запросов - пропускаем как обычно
    return fetch(request);
  }
};
```

4. **Настрой маршрут (Route):**
   - После создания Worker → Settings → Triggers → Routes
   - Add route
   - **Route:** `proteinanalysis.pages.dev/*`
   - **Zone:** (если домен добавлен в Cloudflare) или оставь пустым
   - Сохрани

**⚠️ Если не можешь добавить Route для `.pages.dev`:**
- `.pages.dev` домены управляются Cloudflare Pages, не через Workers Routes
- Используй **Способ 2** (удаление домена) или **Способ 3** (через Pages)

5. **Проверь:**
   - Открой в браузере: `https://proteinanalysis.pages.dev/`
   - Должен автоматически редиректить на `https://seqanalysis.org/`
   - Проверь несколько страниц:
     - `https://proteinanalysis.pages.dev/reverse-complement` → `https://seqanalysis.org/reverse-complement`
     - `https://proteinanalysis.pages.dev/orf-finder` → `https://seqanalysis.org/orf-finder`

**Плюсы:**
- ✅ Работает для `.pages.dev` доменов
- ✅ Правильный 301 редирект
- ✅ Передает SEO вес
- ✅ Сохраняет пути и параметры

---

### Способ 2: Удалить старый домен из Cloudflare Pages (РАБОТАЕТ ГАРАНТИРОВАННО) ⭐⭐

**Для `.pages.dev` доменов это самый простой способ:**

**Если Worker не работает или не можешь настроить:**

1. Cloudflare Dashboard → Pages → проект `proteinanalyse`
2. Custom domains → найди `proteinanalysis.pages.dev`
3. Удали домен (Remove domain)

**Что произойдет:**
- Старый домен перестанет работать (вернется на дефолтную страницу Cloudflare)
- Google перестанет индексировать старый домен (через 1-2 недели)
- Весь трафик перейдет на новый домен через canonical теги

**Плюсы:**
- ✅ Работает гарантированно для `.pages.dev` доменов
- ✅ Google перестанет показывать старый домен в результатах
- ✅ Весь трафик перейдет на новый домен (через canonical)

**Минусы:**
- ⚠️ Нет редиректа - пользователи получат дефолтную страницу Cloudflare
- ⚠️ Медленнее передача SEO веса (но все равно передастся через canonical)

**Рекомендация:** Если Worker не работает, используй этот способ - он гарантированно работает для `.pages.dev` доменов.

---

### Способ 3: Оставить как есть (НЕ РЕКОМЕНДУЕТСЯ)

**Если ничего не работает или не хочешь ничего менять:**

- Оставь оба домена работающими
- Canonical теги уже настроены правильно
- Google постепенно переиндексирует на новый домен (1-2 месяца)

**Минусы:**
- ⚠️ Трафик размазывается между двумя доменами
- ⚠️ Медленнее передача SEO веса
- ⚠️ Теряешь 90% трафика сейчас

---

### Способ 3: Удалить старый домен из Cloudflare Pages (НЕ РЕКОМЕНДУЕТСЯ)

**Если ничего не работает:**

1. Cloudflare Dashboard → Pages → проект
2. Custom domains → удали `proteinanalysis.pages.dev`
3. Старый домен перестанет работать

**Минусы:**
- ⚠️ Старый домен вернется на дефолтную страницу Cloudflare (404)
- ⚠️ Пользователи получат ошибку вместо редиректа
- ⚠️ Трафик потеряется полностью

**Лучше использовать Способ 1** - редирект через Page Rules.

---

## 📊 Что будет после настройки

### Текущее состояние:
- Старый домен: **1.64k impressions** (90% трафика)
- Новый домен: **174 impressions** (10% трафика)
- **Потеря: 90% трафика**

### После редиректа (через 1-2 недели):
- Новый домен: **~1.8k impressions** (весь трафик объединен)
- Старый домен: **~0 impressions** (только прямой трафик)
- **Прирост: +900% трафика на новом домене**

### Через 1-2 месяца:
- Новый домен получит SEO вес от старого
- Позиции улучшатся: 38.7 → 17.9+ (уровень старого домена)
- Клики вырастут: 0 → 9+ (как на старом домене)

---

## ✅ Чеклист

- [ ] Открыл Cloudflare Dashboard
- [ ] Добавил домен `proteinanalysis.pages.dev` (если нужно)
- [ ] Создал Page Rule для редиректа 301
- [ ] Проверил что редирект работает в браузере
- [ ] Проверил несколько страниц (главная, reverse-complement, orf-finder)
- [ ] Подождал 1-2 недели
- [ ] Проверил в Google Search Console что трафик переходит на новый домен

---

## 🔗 Полезные ссылки

- **Cloudflare Dashboard:** https://dash.cloudflare.com/
- **Page Rules:** https://dash.cloudflare.com/page-rules
- **Google Search Console (старый):** https://search.google.com/search-console
- **Google Search Console (новый):** https://search.google.com/search-console

---

**⚠️ ВАЖНО:** Перед действиями прочитай `SAFE_DOMAIN_MIGRATION.md` - там безопасный подход без риска потери трафика.

**Альтернатива:** Оставь оба домена работающими и подожди 2-3 месяца - Google сам переиндексирует на новый домен через canonical теги. Это безопаснее. 🚀

