# Сравнение индексации: старый vs новый домен

## 📊 Данные по доменам

### Старый домен: `proteinanalysis.pages.dev`

**Статус индексации:**
- ✅ **Indexed:** 25 страниц
- ❌ **Not indexed:** 11 страниц (4 причины)

**Причины "Not indexed":**
1. **"Crawled - currently not indexed"** - 3 страницы (Validation: Failed)
2. **"Page with redirect"** - 4 страницы (Validation: Not Started)
3. **"Alternative page with proper canonical tag"** - 1 страница (Validation: Not Started)
4. **"Discovered - currently not indexed"** - 3 страницы (Validation: Not Started)

**График:** Резкий рост индексации с 27/01/2026 (с 0 до 25 страниц)

---

### Новый домен: `seqanalysis.org`

**Статус индексации:**
- ✅ **Indexed:** 3 страницы
- ❌ **Not indexed:** 5 страниц (2 причины)

**Причины "Not indexed":**
1. **"Redirect error"** - 3 страницы (Validation: Not Started)
   - `dna-gc-calculator.html`
   - `orf-finder.html`
   - `reverse-complement.html`
2. **"Page with redirect"** - 2 страницы (Validation: Started)
   - `http://seqanalysis.org/` → HTTPS редирект
   - `https://seqanalysis.org/index.html` → root редирект

---

## 🔍 Анализ

### Что хорошо:

1. **Старый домен (`pages.dev`):**
   - ✅ 25 страниц проиндексированы
   - ✅ Хорошая индексация после 27/01/2026
   - ⚠️ 11 страниц не проиндексированы (но это нормально для начала)

2. **Новый домен (`seqanalysis.org`):**
   - ✅ 3 страницы проиндексированы
   - ✅ Canonical теги настроены правильно
   - ⚠️ 5 страниц не проиндексированы (но меньше проблем, чем на старом)

### Что нужно исправить:

1. **"Redirect error" на новом домене:**
   - 3 страницы с ошибкой редиректа:
     - `dna-gc-calculator.html` → возвращает **308** (редирект на `/dna-gc-calculator`)
     - `orf-finder.html` → возвращает **308** (редирект на `/orf-finder`)
     - `reverse-complement.html` → возвращает **308** (редирект на `/reverse-complement`)
   
   **Причина:** Cloudflare Pages автоматически редиректит `.html` файлы на версии без расширения (Pretty URLs). Это стандартное поведение платформы.
   
   **Решение:**
   - ✅ Версии без `.html` возвращают **200 OK** и работают нормально
   - ⚠️ Это не критично для SEO - Google может индексировать версии без `.html` (clean URLs даже лучше)
   - 📝 **Рекомендация:** Обновить canonical теги, чтобы они указывали на версии без `.html` (например, `https://seqanalysis.org/dna-gc-calculator` вместо `https://seqanalysis.org/dna-gc-calculator.html`)
   - 📝 Или отключить "Pretty URLs" в настройках Cloudflare Pages Dashboard → Settings → Builds & deployments → Pretty URLs (если требуется)
   
   **Проверка (выполнена):**
   - ❌ `https://seqanalysis.org/dna-gc-calculator.html` → **308** (редирект на `/dna-gc-calculator`)
   - ✅ `https://seqanalysis.org/dna-gc-calculator` → **200 OK**
   - ❌ `https://seqanalysis.org/orf-finder.html` → **308** (редирект на `/orf-finder`)
   - ✅ `https://seqanalysis.org/orf-finder` → **200 OK**
   - ❌ `https://seqanalysis.org/reverse-complement.html` → **308** (редирект на `/reverse-complement`)
   - ✅ `https://seqanalysis.org/reverse-complement` → **200 OK**

2. **"Page with redirect" на обоих доменах:**
   - Это нормально (HTTP→HTTPS, index.html→root)
   - Validation уже Started/Not Started - Google проверяет
   - Обычно проходит автоматически

---

## 📈 Прогноз

**После исправлений:**
- Новый домен должен достичь уровня старого (25+ проиндексированных страниц)
- Ошибки редиректа должны исчезнуть
- SEO вес со старого домена передастся на новый через редирект 301

**Временные рамки:**
- Исправления мобильной версии → улучшение CTR → больше кликов
- Исправления редиректов → больше проиндексированных страниц
- Результаты через 1-2 недели после деплоя

---

## ✅ Рекомендации

1. **Статус деплоя исправлений:**
   - ✅ `_middleware.js` для редиректа со старого домена - **задеплоен** (коммит `28d1490` запушен в `origin/master`)
   - ✅ Исправления мобильной версии (кнопки, прокрутка) - **задеплоены** (коммит `933b089`)
   
   **Как проверить работу middleware:**
   - Откройте `https://proteinanalysis.pages.dev/index.html` в браузере
   - Должен произойти автоматический редирект на `https://seqanalysis.org/index.html`
   - Или проверьте через инструменты разработчика (Network tab) - должен быть статус `301 Moved Permanently`

2. **В Google Search Console:**
   - Для нового домена: Redirect error → VALIDATE FIX
   - Запросить переиндексацию для проблемных страниц

3. **Мониторинг:**
   - Проверять через 1-2 недели
   - Сравнивать прогресс между доменами
   - Отслеживать передачу SEO веса со старого на новый

---

## 🎯 Итог

**Старый домен:** Хорошо проиндексирован (25 страниц), но это временный домен  
**Новый домен:** Меньше проиндексированных страниц (3), но это основной домен

**Цель:** Перевести весь трафик и индексацию на новый домен через редирект 301.
