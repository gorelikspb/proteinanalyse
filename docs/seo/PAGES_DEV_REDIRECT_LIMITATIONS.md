# Ограничения редиректа для .pages.dev доменов

## 🔍 Проблема

**Обнаружено:**
- Cloudflare Page Rules работают **только для купленных доменов** (`.org`, `.com`, `.net` и т.д.)
- Для `.pages.dev` доменов Page Rules **недоступны**
- Нужны альтернативные способы редиректа

---

## ✅ Доступные решения для .pages.dev

### Вариант 1: Удалить домен из Cloudflare Pages (РЕКОМЕНДУЕТСЯ) ⭐⭐⭐

**Как сделать:**
1. Cloudflare Dashboard → Pages → проект
2. Custom domains → найди `proteinanalysis.pages.dev`
3. Удали домен (Remove domain)

**Что произойдет:**
- Старый домен перестанет работать
- Вернется на дефолтную страницу Cloudflare
- Google перестанет индексировать старый домен (через 1-2 недели)
- Весь трафик перейдет на новый домен через canonical теги

**Плюсы:**
- ✅ Работает гарантированно
- ✅ Просто и быстро
- ✅ Google перестанет показывать старый домен

**Минусы:**
- ⚠️ Нет редиректа (пользователи получат дефолтную страницу)
- ⚠️ Медленнее передача SEO веса

---

### Вариант 2: Cloudflare Workers (может не работать)

**Проблема:**
- Workers Routes работают только для доменов, добавленных в Cloudflare как отдельные сайты
- `.pages.dev` домены управляются Cloudflare Pages, не через Workers Routes
- Может не работать для `.pages.dev` доменов

**Если попробовать:**
1. Создай Worker с кодом редиректа
2. Попробуй добавить Route для `proteinanalysis.pages.dev/*`
3. Если не получается - используй Вариант 1

---

### Вариант 3: Оставить как есть

**Если ничего не работает:**
- Оставь оба домена работающими
- Canonical теги уже настроены правильно
- Google постепенно переиндексирует на новый домен (1-2 месяца)

**Минусы:**
- ⚠️ Трафик размазывается между двумя доменами
- ⚠️ Медленнее передача SEO веса

---

## 🎯 Рекомендация

**Для `.pages.dev` доменов:**

1. **Удали старый домен из Cloudflare Pages** (Вариант 1)
2. Google перестанет индексировать старый домен через 1-2 недели
3. Весь трафик перейдет на новый домен через canonical теги
4. SEO вес передастся новому домену (медленнее, но передастся)

**Это самый простой и надежный способ для `.pages.dev` доменов.**

---

## 📊 Ожидаемые результаты

### После удаления старого домена:

**Через 1-2 недели:**
- Google перестанет показывать старый домен в результатах
- Impressions на старом домене упадут до 0
- Impressions на новом домене вырастут (весь трафик перейдет)

**Через 1-2 месяца:**
- Новый домен получит весь трафик (1.64k+ impressions)
- SEO вес передастся через canonical теги
- Позиции улучшатся

---

## 🔗 Полезные ссылки

- **Cloudflare Pages:** https://dash.cloudflare.com/pages
- **Google Search Console:** https://search.google.com/search-console

---

**Готово!** Для `.pages.dev` доменов удаление из Custom domains - самый простой способ. 🚀

