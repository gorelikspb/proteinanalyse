# Миграция со старого домена на новый

## 🔍 Текущая ситуация

**Наблюдение:**
- Старый домен `proteinanalysis.pages.dev` показывает резкий рост Impressions (1.64k за 7 дней)
- Новый домен `seqanalysis.org` только начинает индексироваться (2 страницы)
- Рост старого домена совпал по времени с регистрацией нового домена

**Проблема:**
- Старый домен получает больше трафика, но это не основной домен
- Canonical теги указывают на новый домен, но Google все еще индексирует старый
- Трафик "размазывается" между двумя доменами

---

## 🎯 Почему это происходит

### Причины роста старого домена:

1. **Google увидел изменения и начал активнее индексировать**
   - Canonical теги были добавлены (31 января 2026) - сигнал для Google
   - Sitemap был обновлен - триггер для активной индексации
   - Новый домен добавлен в Search Console - Google начал проверять оба домена
   - **Результат:** Google начал активнее краулить старый домен после обнаружения изменений

2. **Google обнаружил связь между доменами**
   - Canonical теги указывают на новый домен
   - Google понял: "это один сайт на разных доменах"
   - Google начал активнее индексировать оба домена чтобы понять связь
   - **Результат:** Активная индексация обоих доменов одновременно

3. **"Эффект свежести" контента**
   - Изменения в коде (canonical, sitemap) = сигнал для Google
   - Google активнее индексирует обновленные сайты
   - Это нормальное поведение - помогает находить новый контент
   - **Результат:** Резкий рост Impressions после изменений

4. **"Зрелость" домена в Google**
   - Старый домен уже был проиндексирован ранее (с декабря 2024)
   - Google доверяет ему больше чем новому
   - История индексации помогает в ранжировании
   - **Результат:** Старый домен получает больше трафика

**Вывод:** Рост трафика - это **НОРМАЛЬНОЕ** поведение Google после обнаружения изменений. Это НЕ совпадение - есть прямая связь между регистрацией нового домена, добавлением canonical тегов и ростом трафика на старом домене.

---

## ✅ Что делать

### Вариант 1: Редирект 301 со старого на новый (РЕКОМЕНДУЕТСЯ) ⭐⭐⭐

**Почему это лучше:**
- ✅ Передает весь SEO вес новому домену
- ✅ Объединяет трафик на одном домене
- ✅ Пользователи всегда попадают на правильный домен
- ✅ Google быстрее переиндексирует на новый домен

**Как сделать:**

1. **В Cloudflare Pages:**
   - Pages → Custom domains → `proteinanalysis.pages.dev`
   - Настрой редирект на `seqanalysis.org` (если есть такая опция)
   - Или удали старый домен из Custom domains

2. **Через Cloudflare Workers или Page Rules:**
   - Создай правило редиректа: `proteinanalysis.pages.dev/*` → `seqanalysis.org/*` (301)

3. **Через `_redirects` файл (если работает для custom domains):**
   ```
   # Redirect old domain to new domain
   https://proteinanalysis.pages.dev/* https://seqanalysis.org/:splat 301
   ```

**Результат:**
- Весь трафик со старого домена пойдет на новый
- SEO вес передастся новому домену
- Google быстрее переиндексирует на новый домен

---

### Вариант 2: Оставить оба домена работающими

**Когда использовать:**
- Если хочешь сохранить оба домена активными
- Если старый домен используется где-то еще

**Что нужно сделать:**
1. ✅ Убедись что canonical теги правильные (уже сделано)
2. ✅ Убедись что sitemap указывает на новый домен (уже сделано)
3. ⏳ Подожди 1-2 месяца - Google постепенно переиндексирует на новый

**Минусы:**
- ⚠️ Трафик размазывается между двумя доменами
- ⚠️ Медленнее передача SEO веса
- ⚠️ Пользователи могут попадать на старый домен

---

## 📊 Анализ текущих данных

### Старый домен (`proteinanalysis.pages.dev`):
- **Impressions:** 1.64k за 7 дней (растет радикально)
- **Clicks:** 9 (низкий CTR 0.5%)
- **Position:** 17.9 (средняя позиция)
- **Топ страницы:**
  - `/articles/codon-optimization-guide` - 455 impressions, позиция 10.5
  - `/reverse-complement` - 358 impressions, позиция 27.0
  - `/sequence-translation` - 200 impressions, позиция 20.1
- **Топ запросы:**
  - "reverse complement" - 47 impressions, позиция 45.0
  - "reverse complement tool" - 45 impressions, позиция 11.3
  - "protein translation tool" - 23 impressions, позиция 9.5

### Новый домен (`seqanalysis.org`):
- **Impressions:** 174 за 7 дней (в 9 раз меньше!)
- **Clicks:** 0 (CTR 0%)
- **Position:** 38.7 (хуже позиции)
- **Топ страницы:**
  - `/orf-finder` - 82 impressions, позиция 40.2
  - `/reverse-complement` - 69 impressions, позиция 41.9
  - `/` (главная) - 21 impressions, позиция 25.4
- **Проиндексировано:** 2-4 страницы (только началась индексация)

**Вывод:** 
- Старый домен получает **в 9 раз больше** видимости (1.64k vs 174 impressions)
- Старый домен имеет **лучшие позиции** (17.9 vs 38.7)
- Старый домен получает **клики** (9 vs 0)
- **Это критично!** Нужно срочно настроить редирект чтобы передать весь трафик на новый домен.

---

## 🎯 Рекомендация (ОБНОВЛЕННАЯ)

**⚠️ ВАЖНО: Рассмотри безопасный подход перед действиями.**

**См. также:** `SAFE_DOMAIN_MIGRATION.md` - консервативный подход без риска потери трафика.

**Почему срочно:**
- Старый домен получает **в 9 раз больше трафика** (1.64k vs 174 impressions)
- Старый домен имеет **лучшие позиции** (17.9 vs 38.7)
- Старый домен получает **клики** (9 vs 0 на новом)
- **Теряешь трафик!** Пользователи попадают на старый домен вместо нового

**Причины:**
1. ✅ Объединяет весь трафик на одном домене (сейчас теряется 90% трафика!)
2. ✅ Передает SEO вес новому домену (старый имеет лучшие позиции)
3. ✅ Пользователи всегда попадают на правильный домен
4. ✅ Google быстрее переиндексирует на новый домен
5. ✅ **ОСТАНОВИТ потерю трафика** - сейчас 90% трафика идет на старый домен

**Как сделать:**

### Способ 1: Через Cloudflare Workers (РЕКОМЕНДУЕТСЯ для .pages.dev) ⭐⭐⭐

**⚠️ Важно:** Page Rules работают только для купленных доменов. Для `.pages.dev` доменов нужно использовать Workers.

1. Cloudflare Dashboard → **Workers & Pages** → Create → **Worker**
2. Имя: `redirect-old-domain`
3. Вставь код:
```javascript
export default {
  async fetch(request) {
    const url = new URL(request.url);
    
    if (url.hostname === 'proteinanalysis.pages.dev') {
      const path = url.pathname;
      const search = url.search;
      const newUrl = `https://seqanalysis.org${path}${search}`;
      return Response.redirect(newUrl, 301);
    }
    
    return fetch(request);
  }
};
```
4. Settings → Triggers → Routes → Add route
5. Route: `proteinanalysis.pages.dev/*`
6. Сохрани

**Плюсы:**
- ✅ Работает для `.pages.dev` доменов
- ✅ Правильный 301 редирект
- ✅ Передает SEO вес
- ✅ Сохраняет пути и параметры

### Способ 2: Удалить старый домен из Cloudflare Pages

1. Cloudflare Dashboard → Pages → проект
2. Custom domains → удали `proteinanalysis.pages.dev`
3. Старый домен перестанет работать (или вернется на дефолтную страницу Cloudflare)

**Минусы:**
- ⚠️ Старый домен перестанет работать (не редиректит)
- ⚠️ Пользователи получат 404 или дефолтную страницу

**Лучше использовать Способ 1** - редирект через Page Rules.

---

## ⏱️ Ожидаемые результаты

### После настройки редиректа:

**Через 1-2 недели:**
- Google начнет видеть редиректы
- Трафик со старого домена (1.64k impressions) пойдет на новый
- Impressions на новом домене вырастут с 174 до ~1.8k+ (объединенный трафик)
- Позиции улучшатся (получит SEO вес от старого домена)

**Через 1-2 месяца:**
- Большинство трафика будет на новом домене
- Старый домен будет показывать меньше Impressions (только прямой трафик)
- SEO вес полностью передастся новому домену
- Позиции нового домена улучшатся до уровня старого (17.9 → лучше)

**Через 3-6 месяцев:**
- Новый домен получит весь трафик (1.64k+ impressions)
- Старый домен можно будет удалить или оставить как резервный
- Новый домен будет иметь лучшие позиции чем сейчас (38.7 → 17.9+)

**Текущая потеря трафика:**
- Сейчас теряется **~90% трафика** (1.64k идет на старый, 174 на новый)
- После редиректа весь трафик (1.8k+) будет на новом домене

---

## 📋 Чеклист действий

- [ ] Решил: редиректить старый домен на новый (рекомендуется)
- [ ] Настроил Page Rule для редиректа 301
- [ ] Проверил что редирект работает (открой `proteinanalysis.pages.dev` в браузере)
- [ ] Подождал 1-2 недели
- [ ] Проверил в Google Search Console что трафик переходит на новый домен
- [ ] (Опционально) Удалил старый домен из Custom domains через 3-6 месяцев

---

## 🔗 Полезные ссылки

- **Cloudflare Page Rules:** https://dash.cloudflare.com/page-rules
- **Google Search Console (старый домен):** https://search.google.com/search-console
- **Google Search Console (новый домен):** https://search.google.com/search-console

---

**Готово!** После настройки редиректа весь трафик и SEO вес перейдут на новый домен. 🚀

