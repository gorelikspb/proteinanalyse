# ⚙️ Настройка домена в Cloudflare после покупки

## 📋 Что нужно сделать в Cloudflare

### Шаг 1: Проверить статус домена в Pages

1. Cloudflare Dashboard → **Pages** → проект `proteinanalyse`
2. Вкладка **"Custom domains"**
3. Проверь статус домена `seqanalysis.org`:
   - ✅ **"Active"** (зеленый) - все готово, ничего делать не нужно
   - 🟡 **"Verifying"** (желтый) - нужно подождать или проверить DNS
   - ❌ **"Failed"** (красный) - нужно исправить DNS

---

### Шаг 2: Если статус "Verifying"

**Вариант A: Домен куплен через Cloudflare Registrar**

Если домен куплен через Cloudflare, DNS должен настроиться автоматически:
1. **Просто подожди 5-15 минут**
2. Cloudflare автоматически проверит DNS записи
3. Статус изменится на "Active"
4. Ты получишь email когда домен активирован

**Ничего делать не нужно** - просто жди.

---

**Вариант B: Нужно добавить CNAME вручную**

Если Cloudflare показывает инструкции по добавлению CNAME:

1. Cloudflare Dashboard → твой домен `seqanalysis.org` → **DNS** → **Records**
2. Проверь есть ли CNAME запись:
   - Имя: `@` (или пусто)
   - Цель: `proteinanalyse.pages.dev` (Cloudflare Pages project)
3. Если записи нет - добавь:
   - Нажми **"Add record"**
   - Тип: **CNAME**
   - Имя: `@` (или оставь пустым для корневого домена)
   - Цель: `proteinanalyse.pages.dev` (указано в Pages → Custom domains)
   - Proxy status: **Proxied** (оранжевое облачко)
   - Сохрани
4. Вернись в Pages → Custom domains → нажми **"Check DNS records"**

---

### Шаг 3: Если есть кнопка "Complete DNS setup"

1. Нажми **"Complete DNS setup"** (синяя ссылка рядом с "Verifying")
2. Следуй инструкциям
3. Обычно это просто подтверждение что nameservers правильные

---

### Шаг 4: Проверить что домен работает

**Через 5-15 минут после настройки:**

1. Открой в браузере: `https://seqanalysis.org`
2. Должен открыться твой сайт
3. Должен быть зеленый замочек (SSL)

**Если не работает:**
- Подожди еще 10-15 минут (DNS распространяется)
- Проверь CNAME запись в DNS
- Проверь что Cloudflare Pages проект активен

---

## ✅ Что должно быть настроено

### В Cloudflare DNS (домен `seqanalysis.org`):

1. **CNAME запись:**
   - Имя: `@` (или пусто)
   - Цель: `proteinanalyse.pages.dev` (Cloudflare Pages)
   - Proxy: **Proxied** (оранжевое облачко)

2. **Автоматические записи:**
   - Cloudflare может добавить еще записи автоматически
   - Не удаляй их

### В Cloudflare Pages:

1. **Custom domains:**
   - `seqanalysis.org` - статус "Active" (зеленый)

---

## 🎯 Итоговый чеклист

- [ ] Домен куплен через Cloudflare
- [ ] Домен добавлен в Cloudflare Pages → Custom domains
- [ ] Статус домена: "Active" (или "Verifying" и ждешь)
- [ ] CNAME запись добавлена (если нужно)
- [ ] Сайт открывается по новому домену
- [ ] SSL работает (зеленый замочек)

---

## ⏱️ Временная шкала

- **Сразу:** Домен добавлен в Pages
- **5-15 минут:** DNS проверка, статус меняется на "Active"
- **1-2 часа:** DNS распространится по всему миру
- **После этого:** Сайт доступен по новому домену

---

## 🆘 Если что-то не работает

### Домен все еще "Verifying" через 30 минут:

1. Проверь CNAME запись в DNS
2. Нажми "Check DNS records" в Pages
3. Подожди еще 15 минут
4. Если не помогает - свяжись с поддержкой Cloudflare

### Сайт не открывается:

1. Проверь что CNAME запись правильная
2. Проверь что Cloudflare Pages проект активен
3. Подожди 1-2 часа (DNS распространяется)
4. Очисти кэш браузера (Ctrl+F5)

### SSL не работает:

1. Cloudflare автоматически выдает SSL
2. Подожди 5-10 минут после активации домена
3. Проверь что домен "Proxied" (оранжевое облачко в DNS)

---

**Готово!** Если статус "Verifying" - просто подожди 5-15 минут, Cloudflare все настроит автоматически. 🚀






