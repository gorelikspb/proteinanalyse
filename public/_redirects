# Cloudflare Pages redirects
# Format: /old-path /new-path [status code]

# Redirect index.html to root (clean URLs)
/index.html / 301

# Note: URLs without .html extension are NOT redirected
# Google will index only .html versions (canonical tags are set)
# This avoids redirect errors and duplicate content issues
