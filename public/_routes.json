{
  "version": 1,
  "include": ["/api/*"],
  "exclude": []
}

// Note: Middleware (_middleware.js) runs on ALL requests regardless of _routes.json
// _routes.json only controls which routes are handled by Functions, not middleware








