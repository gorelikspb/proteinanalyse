# Лог автоматизации и инфраструктуры

## 📋 Обзор

Этот лог документирует все решения, связанные с:
- Деплоем и хостингом (GitHub Pages, Cloudflare Pages)
- Автоматизацией (скрипты, CI/CD)
- Инструментами разработки (Puppeteer, скриншоты)
- Аналитикой (Microsoft Clarity)
- Организацией приватных инструкций и конфигов

**Важно**: Этот лог не касается функциональных решений проекта, только инфраструктура.

---

## 🗓️ История решений по автоматизации

### Этап 1: Структура для GitHub Pages

**Что было**: Файлы в корне репозитория.

**Решение**: Все файлы для сайта перемещены в папку `/docs`

**Почему такое решение**:
- GitHub Pages может деплоить из `/docs`
- Можно хранить другие файлы (README, конфиги) в корне
- Чистое разделение между кодом сайта и документацией проекта

**Плюсы**:
- ✅ Стандартная практика для GitHub Pages
- ✅ Чистая структура репозитория
- ✅ Можно добавить исходники/тесты в корень
- ✅ Легко переключиться на другой хостинг

**Минусы**:
- ❌ URL содержит `/docs/` (но это не критично)
- ❌ Нужно помнить, что файлы в `/docs`

**Подводные камни**:
- Нужно было обновить все пути к файлам (styles.css, common.js)
- Важно было не забыть про относительные пути

---

### Этап 2: Подготовка к деплою на Cloudflare Pages

**Дата**: Текущий этап

**Что было**: Проект был настроен для GitHub Pages из папки `/docs`, но возникла необходимость перейти на Cloudflare Pages для лучшей производительности и более гибкой настройки.

**Решение**: 
1. **Перемещение файлов**: Создана папка `/public` и все файлы из `/docs` перемещены туда
2. **Автоматизация настройки**: Создан скрипт `instructions/scripts/setup-cloudflare-pages-auto.ps1` для автоматической настройки
3. **Проверка доступности доменов**: Скрипт проверяет доступность нескольких вариантов доменных имён:
   - `proteinanalysis.pages.dev`
   - `bioseq.pages.dev`
   - `sequence-lab.pages.dev`
   - `protein-tools.pages.dev`
   - `dna-analysis.pages.dev`
   - `seqanalysis.pages.dev`
4. **Организация конфигурации**: Все чувствительные данные (API токены, Account ID, Member ID) хранятся в `instructions/.cloudflare-config.local`
5. **Документация**: Создана подробная инструкция `instructions/CLOUDFLARE_PAGES_SETUP.md` с тремя вариантами настройки

**Плюсы**:
- ✅ Автоматизация экономит время
- ✅ Проверка доступности доменов перед созданием проекта
- ✅ Безопасное хранение токенов (папка `instructions/` в `.gitignore`)
- ✅ Гибкость выбора доменного имени
- ✅ Подробная документация для разных сценариев

**Минусы**:
- ⚠️ Подключение GitHub репозитория всё ещё требует ручного шага (OAuth авторизация)
- ⚠️ PowerShell скрипты работают только на Windows (но можно адаптировать)

**Подводные камни**:
- API токен должен иметь правильные права доступа
- Account ID может быть пустым, если токен не имеет доступа к аккаунтам
- Некоторые доменные имена могут быть заняты другими проектами

**Обоснование выбора Cloudflare Pages**:
- ✅ Бесплатный хостинг с CDN
- ✅ Автоматический деплой из Git
- ✅ Быстрая загрузка (глобальная CDN сеть)
- ✅ Простая настройка кастомных доменов
- ✅ Хорошая производительность для статических сайтов

**Альтернатива**: GitHub Pages
- ✅ Проще настройка (уже было настроено)
- ❌ Медленнее загрузка (нет глобального CDN)
- ❌ Меньше возможностей для оптимизации

**Технические детали**:
- Скрипт использует Cloudflare API v4
- Проверяет существующие проекты перед созданием
- Автоматически определяет Account ID из токена
- Настраивает build output directory: `public`
- Build command оставляется пустым (статический сайт)

**Скрипты автоматизации**:
- `instructions/scripts/setup-cloudflare-pages-auto.ps1` - автоматическая настройка с выбором домена
- `instructions/scripts/setup-cloudflare-pages.ps1` - настройка с указанием имени вручную
- `instructions/scripts/setup-cloudflare-pages.sh` - Bash-версия
- `instructions/scripts/get-cloudflare-account-id.ps1` - получение Account ID из API токена
- `instructions/scripts/check-domains.ps1` - проверка доступности доменных имён
- `instructions/scripts/create-project.ps1` - создание проекта через API

---

### Этап 3: Продакшн-деплой на Cloudflare Pages + аналитика

**Дата**: текущий этап

**Что было**: Подготовка завершена, но сайт ещё не был задеплоен и не было аналитики.

**Решение**:
1. Через UI Cloudflare Pages (`+ Add → Pages → Import Git repository`) выбран репозиторий `gorelikspb/proteinanalyse`
2. Вручную задан проект (имя выбиралось из списка доступных) и настроен build:
   - Production branch: `master`
   - Build command: _пусто_
   - Build output directory: `public`
3. Деплой прошёл успешно — сайт теперь доступен по выбранному домену `*.pages.dev`
4. Получен реальный Microsoft Clarity Project ID `u5y6bxpy0v`
5. Сниппет Clarity вставлен во все страницы в `public/` (6 файлов) вместо заглушки

**Плюсы**:
- ✅ Есть рабочий публичный сайт с автоматическим деплоем из Git
- ✅ Логирование пользовательского поведения через Clarity
- ✅ Всё сделано через UI, без необходимости править токены Cloudflare API

**Минусы / ограничения**:
- ⚠️ Имя проекта нужно подбирать вручную (если занято, UI предложит другое)
- ⚠️ Привязка GitHub выполняется только через браузер (OAuth)

**Подводные камни**:
- Необходимо помнить про пустой build command и директорию `public`
- После замены Clarity ID важно очистить старые TODO, чтобы не вернуть заглушку

**Доп. действия**:
- Обновлены инструкции (`CLOUDFLARE_PAGES_SETUP.md`) о навигации `+ Add → Pages`
- В репозитории добавлены вспомогательные скрипты для проверки доменных имён и автосоздания проектов (для будущего использования, когда появится рабочий API token)

---

### Этап 4: Подготовка репозитория к публикации

**Что было**: Понадобилось выложить проект на GitHub, но в репозитории находились внутренние инструкции, журналы и скриншоты, которые не должны быть публичными.

**Решение**:
1. Инициализирован git-репозиторий и сделан первый коммит
2. Добавлен `.gitignore`, исключив `instructions/`, `screenshots/`, `PROJECT_LOG.md`, `AUTOMATION_LOG.md`, `node_modules/`, утилиты (`scripts/`) и прочие служебные файлы
3. Удалены из истории все приватные материалы и запушен только продуктовый код
4. Зафиксировано в `instructions/PUBLIC_REPO_SETUP.md` и `instructions/GITHUB_SETUP.md`, что служебные файлы всегда остаются локально

**Почему такое решение**:
- Хотим оставлять на GitHub только то, что необходимо пользователям (лендинг и связанный код)
- Инструкции и журналы содержат рабочие заметки и должны храниться локально
- Удаление лишнего облегчает размер репо и защищает внутренние процессы

**Плюсы**:
- ✅ Репозиторий чистый, подходит для публичного просмотра
- ✅ Нет риска случайно раскрыть служебную информацию
- ✅ Простая инструкция для последующих проектов

**Минусы**:
- ❌ Нужно синхронизировать инструкции вручную между проектами
- ❌ Скриншоты и логи не доступны онлайн (только локально)

**Подводные камни**:
- Следить, чтобы `.gitignore` всегда обновлялся при появлении новых служебных файлов
- При коммитах удостоверяться, что `git status` пуст от приватных данных
- Использовать `pre-push-check.ps1` или `pre-push-check.sh` для автоматической проверки

**Что сделано**:
- ✅ Добавлен `.gitignore` с нужными исключениями
- ✅ Репозиторий проинициализирован и запушен на GitHub
- ✅ В инструкциях описано, что публикуем, а что оставляем локально
- ✅ Созданы скрипты проверки перед push

---

### Этап 5: Организация приватных инструкций и скриптов

**Что было**: Инструкции и скрипты были разбросаны по проекту, некоторые содержали чувствительные данные.

**Решение**:
1. Создана папка `instructions/` для всех приватных инструкций и конфигов
2. Все чувствительные данные (API токены, Account ID, Member ID) хранятся в `instructions/.cloudflare-config.local`
3. Папка `instructions/` добавлена в `.gitignore`
4. Создана подпапка `instructions/scripts/` для переиспользуемых скриптов:
   - PowerShell скрипты для Windows
   - Bash скрипты для Linux/Mac
   - Шаблоны для будущих проектов
5. Создан `pre-push-check.ps1` и `pre-push-check.sh` для проверки перед push в Git
6. Все инструкции документированы в `instructions/README.md`
7. Создан `instructions.zip` для переноса инструкций в новые проекты

**Почему такое решение**:
- Централизованное хранение приватных данных
- Легко переносить в новые проекты
- Защита от случайного коммита чувствительных данных
- Переиспользование скриптов между проектами

**Плюсы**:
- ✅ Безопасность (чувствительные данные не в Git)
- ✅ Организация (всё в одном месте)
- ✅ Переиспользование (скрипты можно копировать)
- ✅ Документация (все инструкции структурированы)

**Минусы**:
- ❌ Нужно помнить про `.gitignore`
- ❌ Нужно проверять перед push

**Подводные камни**:
- Важно не забыть добавить `instructions/` в `.gitignore`
- Нужно регулярно проверять, что чувствительные данные не попадают в репозиторий
- При переносе в новый проект нужно обновить пути в скриптах

**Структура `instructions/`**:
```
instructions/
├── .cloudflare-config.local      # Конфигурация (токены, ID) - НЕ КОММИТИТЬ
├── AI_INSTRUCTIONS.md            # Инструкции для AI-ассистента
├── AGENT_KICKOFF.md              # Быстрый старт для AI
├── AUTOMATED_SCREENSHOTS.md      # Инструкции по автоматизации скриншотов
├── CLARITY_ADD_TO_PROJECT.md     # Инструкции по добавлению Clarity
├── CLOUDFLARE_PAGES_SETUP.md     # Инструкции по настройке Cloudflare Pages
├── GITHUB_SETUP.md               # Инструкции по настройке GitHub
├── PUBLIC_REPO_SETUP.md          # Инструкции по настройке публичного репозитория
├── README.md                     # Обзор всех инструкций
└── scripts/                      # Переиспользуемые скрипты
    ├── check-domains.ps1
    ├── create-project.ps1
    ├── get-cloudflare-account-id.ps1
    ├── setup-cloudflare-pages-auto.ps1
    ├── setup-cloudflare-pages.ps1
    ├── setup-cloudflare-pages.sh
    └── README.md
```

---

### Этап 6: Централизованный радар проектов

**Что было**: Нужно агрегировать прогресс по всем проектам в одном месте (Notion/Airtable), чтобы видеть статусы и следующие шаги.

**Решение**:
1. Создан формат `PROJECTS_OVERVIEW.json` — структурированный формат, который легко читать скриптами
2. Подготовлена инструкция `instructions/CENTRAL_RADAR_SETUP.md` (если понадобится) с описанием, как собрать данные в отдельном репозитории
3. Подготовлен пример скрипта `collect.js`, который скачивает `PROJECTS_OVERVIEW.json` из всех репозиториев и сохраняет агрегированный `dashboard.json`

**Почему такое решение**:
- JSON проще парсить и автоматически синхронизировать
- Статичный Node.js скрипт можно запустить локально, а позже перенести в GitHub Actions или Airtable Automation
- Центральный радар помогает планировать работу по всем проектам

**Плюсы**:
- ✅ Унифицированный формат для всех репозиториев
- ✅ Быстрый способ собрать сводку без ручного копирования таблиц
- ✅ Можно развернуть отдельный агрегатор

**Минусы**:
- ❌ Пока нужно вручную запускать скрипт (не подключено к API Notion/Airtable)
- ❌ Требуется поддерживать список репозиториев (`repos.json`) актуальным

**Подводные камни**:
- Проверять, что `PROJECTS_OVERVIEW.json` всегда коммитится вместе с изменениями
- Если репозиторий приватный и файл недоступен по raw-ссылке, нужно подумать о токенах или исключениях

**Что сделано**:
- ✅ Подготовлен формат `PROJECTS_OVERVIEW.json` для будущего использования
- ✅ Запланирована возможность создания отдельного репозитория для центрального радара

---

### Этап 7: Автоматизация скриншотов для дневника проекта

**Что было**: Нужно было делать скриншоты страниц для `PROJECT_LOG.md` вручную.

**Решение**: Настроена автоматизация через Puppeteer:
1. Инициализирован `npm` в корне проекта (`npm init -y`)
2. Установлен `puppeteer`
3. Создан скрипт `scripts/capture-screenshots.js` для автоматического создания скриншотов
4. Добавлен npm-скрипт `capture:screens` для запуска
5. Скриншоты сохраняются в `screenshots/` с понятными именами

**Почему такое решение**:
- Автоматизация экономит время
- Единообразный вид скриншотов
- Легко обновлять при изменении страниц
- Можно интегрировать в CI/CD

**Плюсы**:
- ✅ Быстрое создание скриншотов
- ✅ Единообразие
- ✅ Легко обновлять
- ✅ Можно автоматизировать в CI/CD

**Минусы**:
- ❌ Нужен Node.js и npm
- ❌ Зависимость от Puppeteer

**Подводные камни**:
- Нужно обновлять скрипт при изменении структуры страниц
- Селекторы могут сломаться при рефакторинге

**Инструкции**: Подробные инструкции по настройке и использованию описаны в `instructions/AUTOMATED_SCREENSHOTS.md`

---

### Этап 8: Интеграция Microsoft Clarity

**Что было**: Нужна аналитика пользовательского поведения на сайте.

**Решение**:
1. Создан проект в Microsoft Clarity
2. Получен Project ID: `u5y6bxpy0v`
3. Сниппет Clarity вставлен во все HTML страницы в `public/`
4. Создана инструкция `instructions/CLARITY_ADD_TO_PROJECT.md` для будущих проектов

**Почему такое решение**:
- Бесплатная аналитика
- Простая интеграция (один скрипт)
- Хорошая визуализация поведения пользователей
- Не требует бэкенда

**Плюсы**:
- ✅ Бесплатно
- ✅ Простая интеграция
- ✅ Хорошая визуализация
- ✅ Не влияет на производительность

**Минусы**:
- ❌ Нужно помнить про Project ID
- ❌ Зависимость от внешнего сервиса

**Подводные камни**:
- Важно не забыть заменить заглушку на реальный Project ID
- Нужно проверить, что скрипт загружается на всех страницах

**Инструкции**: Подробные инструкции по добавлению Clarity описаны в `instructions/CLARITY_ADD_TO_PROJECT.md`

---

## 🎯 Ключевые решения по инфраструктуре

### 1. Почему Cloudflare Pages вместо GitHub Pages?

**Решение**: Переход с GitHub Pages на Cloudflare Pages.

**Обоснование**:
- ✅ Глобальная CDN сеть (быстрее загрузка)
- ✅ Больше возможностей для оптимизации
- ✅ Простая настройка кастомных доменов
- ✅ Автоматический деплой из Git
- ✅ Бесплатный хостинг

**Альтернатива**: GitHub Pages
- ✅ Проще настройка
- ❌ Медленнее загрузка
- ❌ Меньше возможностей

### 2. Почему папка `/public` вместо `/docs`?

**Решение**: Переименование `/docs` в `/public` при переходе на Cloudflare Pages.

**Обоснование**:
- ✅ Стандартное название для статических сайтов
- ✅ Cloudflare Pages использует `/public` по умолчанию
- ✅ Более понятное название

**Альтернатива**: Оставить `/docs`
- ✅ Уже было настроено
- ❌ Нестандартное название для Cloudflare Pages

### 3. Почему папка `instructions/` для приватных данных?

**Решение**: Все приватные инструкции и конфиги в `instructions/`.

**Обоснование**:
- ✅ Централизованное хранение
- ✅ Легко переносить в новые проекты
- ✅ Защита от случайного коммита
- ✅ Чистая структура проекта

**Альтернатива**: Разбросанные файлы
- ❌ Сложнее найти
- ❌ Легче забыть про `.gitignore`
- ❌ Сложнее переносить

### 4. Почему Puppeteer для скриншотов?

**Решение**: Использовать Puppeteer для автоматизации скриншотов.

**Обоснование**:
- ✅ Автоматизация экономит время
- ✅ Единообразный вид
- ✅ Легко обновлять
- ✅ Можно интегрировать в CI/CD

**Альтернатива**: Ручные скриншоты
- ✅ Не нужны зависимости
- ❌ Медленнее
- ❌ Менее единообразно

### 5. Разделение публичного и приватного контента

**Решение**: Чёткое разделение на то, что идёт в GitHub (только продуктовый код), и то, что остаётся локально (инструкции, логи, скриншоты, утилиты).

**Обоснование**:
- ✅ Репозитории остаются чистыми и понятными для внешних пользователей
- ✅ Внутренние процессы не раскрываются
- ✅ Легко копировать структуру в новые проекты
- ✅ Защита от случайного коммита чувствительных данных

**Альтернатива**: Публиковать всё
- ❌ Раскрытие внутренних процессов
- ❌ Риск утечки чувствительных данных
- ❌ Засорение репозитория

---

## 📊 Метрики инфраструктуры

### Деплой
- **Платформа**: Cloudflare Pages
- **Домен**: `*.pages.dev`
- **Build время**: ~30 секунд
- **Автоматический деплой**: Да (из Git)

### Аналитика
- **Сервис**: Microsoft Clarity
- **Project ID**: `u5y6bxpy0v`
- **Интеграция**: Скрипт на всех страницах

### Автоматизация
- **Скрипты**: 6 PowerShell/Bash скриптов
- **Инструкции**: 8 markdown файлов
- **Скриншоты**: Автоматизация через Puppeteer

---

## 🔮 Будущие улучшения инфраструктуры

### Потенциальные доработки:
1. **CI/CD интеграция** - автоматический запуск скриптов при коммите
2. **Автоматизация деплоя через API** - полная автоматизация без UI
3. **Мониторинг производительности** - интеграция с Cloudflare Analytics
4. **A/B тестирование** - через Cloudflare Workers
5. **Автоматические скриншоты в CI/CD** - обновление скриншотов при деплое

### Подводные камни будущих изменений:
- Автоматизация может усложнить отладку
- Нужно балансировать между автоматизацией и простотой
- API токены нужно безопасно хранить

---

## 📝 Технические детали инфраструктуры

### Структура файлов для деплоя:
```
public/                            # Файлы для деплоя (Cloudflare Pages)
├── index.html                    # Главная страница
├── dna-gc-calculator.html        # GC калькулятор
├── reverse-complement.html        # Reverse complement
├── orf-finder.html               # ORF finder
├── protein-mw-calculator.html    # Protein MW
├── sequence-translation.html     # DNA translation
├── common.js                     # Общие функции
└── styles.css                    # Стили
```

### Структура служебных файлов (не публикуются):
```
project/
├── instructions/          # Инструкции для Cursor/AI
├── screenshots/           # Автоматически генерируемые скриншоты
├── PROJECT_LOG.md         # Дневник проекта (локальный)
├── AUTOMATION_LOG.md      # Этот файл (локальный)
├── scripts/               # Утилиты (например, capture-screenshots.js)
└── node_modules/          # Зависимости npm
```

### Структура публичных файлов (идёт в GitHub):
```
project/
├── public/                # Файлы для деплоя
│   ├── index.html
│   ├── styles.css
│   └── ...
├── README.md              # Описание проекта
├── .gitignore             # Исключения для Git
└── PROJECTS_OVERVIEW.json # Статус проекта (для центрального радара, опционально)
```

### Структура инструкций и скриптов:
```
instructions/                     # Приватные инструкции и конфиги
├── .cloudflare-config.local      # Конфигурация (токены, ID) - НЕ КОММИТИТЬ
├── AI_INSTRUCTIONS.md            # Инструкции для AI-ассистента
├── AGENT_KICKOFF.md              # Быстрый старт для AI
├── AUTOMATED_SCREENSHOTS.md      # Инструкции по автоматизации скриншотов
├── CLARITY_ADD_TO_PROJECT.md     # Инструкции по добавлению Clarity
├── CLOUDFLARE_PAGES_SETUP.md     # Инструкции по настройке Cloudflare Pages
├── GITHUB_SETUP.md               # Инструкции по настройке GitHub
├── PUBLIC_REPO_SETUP.md          # Инструкции по настройке публичного репозитория
├── README.md                     # Обзор всех инструкций
└── scripts/                      # Переиспользуемые скрипты
    ├── check-domains.ps1
    ├── create-project.ps1
    ├── get-cloudflare-account-id.ps1
    ├── setup-cloudflare-pages-auto.ps1
    ├── setup-cloudflare-pages.ps1
    ├── setup-cloudflare-pages.sh
    └── README.md
```

### Зависимости для автоматизации:
- **Node.js + npm** - для Puppeteer и скриптов скриншотов
- **GitHub CLI** (`gh`) - для автоматизации работы с GitHub
- **PowerShell** (Windows) или **Bash** (Linux/Mac) - для скриптов деплоя

---

*Лог обновляется по мере развития инфраструктуры проекта*

