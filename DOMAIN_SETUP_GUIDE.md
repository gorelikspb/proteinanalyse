# Полное руководство по покупке и настройке домена

## 🎯 Цель

Купить домен до 10€/год и автоматически настроить его для сайта на Cloudflare Pages.

## 📋 Пошаговая инструкция

### Шаг 1: Проверка доступности доменов

**Автоматически:**
```powershell
cd d:\dev\proteinanalyse
.\scripts\check-domains.ps1
```

**Или вручную:**
1. Зайди в Cloudflare Dashboard: https://dash.cloudflare.com/domains
2. Введи домен в поиск
3. Проверь цену и доступность

**Рекомендуемые варианты (проверено автоматически):**
1. **`seqanalysis.com`** (~9.5€/год) ⭐⭐⭐ **РЕКОМЕНДУЕТСЯ** ✅ ДОСТУПЕН
   - Международный охват (США + Европа) - идеально для партнера в Германии
2. **`seqanalysis.de`** (~5-8€/год) ⭐⭐⭐ **ДЛЯ ГЕРМАНИИ** ✅ ДОСТУПЕН
   - Локальное преимущество, дешевле, но менее универсален
3. **`protein-tools.com`** (~9.5€/год) ⭐⭐⭐ ✅ ДОСТУПЕН
4. `proteinanalysis.org` (~9-11€/год) ⭐⭐ ✅ ДОСТУПЕН
5. `seqanalysis.org` (~9-11€/год) ⭐⭐ ✅ ДОСТУПЕН

**Занятые домены:**
- ❌ `proteinanalysis.com` (занят)
- ❌ `biosequence.com` (занят)
- ❌ `proteinanalysis.de` (занят)

### Шаг 2: Покупка домена через Cloudflare

**Вариант A: Автоматически (если настроен API токен)**
```powershell
.\scripts\purchase-domain-cloudflare.ps1 -Domain "proteinanalysis.com"
```

**Вариант B: Вручную (рекомендуется для первой покупки)**
1. Зайди в Cloudflare Dashboard: https://dash.cloudflare.com/domains
2. Нажми **"Register domains"**
3. Введи домен (например: `proteinanalysis.com`)
4. Проверь цену (должна быть ~9-10€)
5. Добавь в корзину
6. **Настрой платежный метод** (если еще не настроен):
   - Cloudflare Dashboard → Billing → Payment Methods
   - Добавь карту или PayPal
7. Заверши покупку

**Важно:**
- Cloudflare требует настройки платежного метода перед покупкой
- Цена указана за год
- Автоматическое продление включено по умолчанию

### Шаг 3: Настройка DNS и подключение к Pages

**Автоматически:**
```powershell
.\scripts\setup-domain-dns.ps1 -Domain "proteinanalysis.com"
```

**Или вручную:**

1. **Добавь Custom Domain в Cloudflare Pages:**
   - Cloudflare Dashboard → Pages → твой проект
   - Custom Domains → Add domain
   - Введи домен (например: `proteinanalysis.com`)
   - Cloudflare автоматически настроит DNS

2. **Проверь DNS записи:**
   - Cloudflare Dashboard → DNS → Records
   - Должна быть CNAME запись: `@ → proteinanalysis.pages.dev`
   - Или A запись с IP адресом Cloudflare

3. **Подожди распространения DNS:**
   - Обычно 5-30 минут
   - Проверь: `https://proteinanalysis.com` (должен открываться)

### Шаг 4: Обновление файлов сайта

**Автоматически:**
```powershell
.\scripts\update-site-domain.ps1 -NewDomain "proteinanalysis.com"
```

**Что обновляется:**
- `sitemap.xml` - все URL с новым доменом
- `robots.txt` - ссылка на sitemap
- Документация (GOOGLE_INDEXING_STATUS.md и т.д.)

**Вручную нужно обновить:**
- Google Search Console - добавить новое свойство
- Все внешние ссылки (если есть)

### Шаг 5: Настройка Google Search Console

1. Зайди в Google Search Console: https://search.google.com/search-console
2. Нажми **"Add Property"**
3. Выбери **"URL prefix"**
4. Введи новый домен: `https://proteinanalysis.com`
5. Выбери метод верификации: **"HTML tag"**
6. **Код уже есть на всех страницах!** Просто нажми "Verify"
7. Отправь sitemap: `https://proteinanalysis.com/sitemap.xml`

**Важно:**
- Старое свойство (.pages.dev) можно оставить
- Или удалить после перехода на новый домен
- Новый домен нужно индексировать заново (1-2 недели)

### Шаг 6: Настройка редиректа (опционально)

**Если хочешь, чтобы старый домен перенаправлял на новый:**

1. Cloudflare Dashboard → Pages → твой проект
2. Custom Domains → добавь старый домен `proteinanalysis.pages.dev`
3. Настрой редирект в Cloudflare Workers или через правила

**Или оставь оба домена работающими** - это тоже нормально.

---

## 💰 Стоимость

- **Домен .com/.org**: ~9-11€/год
- **Продление**: та же цена (Cloudflare не повышает цену)
- **Дополнительно**: бесплатно (DNS, SSL, WHOIS privacy включены)

**Итого: ~10€/год** ✅

---

## ⚡ Быстрый старт (все автоматически)

```powershell
# 1. Проверь доступность
.\scripts\check-domains.ps1

# 2. Купи домен (если настроен API токен)
.\scripts\purchase-domain-cloudflare.ps1 -Domain "proteinanalysis.com"

# 3. Настрой DNS
.\scripts\setup-domain-dns.ps1 -Domain "proteinanalysis.com"

# 4. Обнови файлы сайта
.\scripts\update-site-domain.ps1 -NewDomain "proteinanalysis.com"
```

---

## 🔧 Требования

### Для автоматической покупки:
- Cloudflare API Token с правами: `Account → Domain Registration → Edit`
- Настроенный платежный метод в Cloudflare
- Файл `instructions\.cloudflare-config.local` с токеном

### Для ручной покупки:
- Аккаунт Cloudflare
- Платежный метод (карта или PayPal)

---

## ✅ Чеклист после покупки

- [ ] Домен куплен и активен
- [ ] DNS настроен (CNAME запись создана)
- [ ] Custom Domain добавлен в Cloudflare Pages
- [ ] Сайт открывается по новому домену
- [ ] SSL сертификат активен (автоматически от Cloudflare)
- [ ] Sitemap.xml обновлен с новым доменом
- [ ] Google Search Console - добавлено новое свойство
- [ ] Google Search Console - отправлен sitemap
- [ ] Старый домен работает или настроен редирект

---

## 🆘 Решение проблем

### Домен не открывается
- Подожди 30-60 минут (DNS распространение)
- Проверь DNS записи в Cloudflare Dashboard
- Проверь Custom Domain в Pages проекте

### Ошибка при покупке через API
- Проверь API токен (нужны права на Domain Registration)
- Проверь платежный метод в Cloudflare Dashboard
- Попробуй купить вручную через Dashboard

### SSL сертификат не работает
- Cloudflare выдает SSL автоматически (подожди 5-10 минут)
- Проверь статус в Cloudflare Dashboard → SSL/TLS

---

## 📝 Примечания

- **Старый домен .pages.dev** продолжит работать параллельно
- Можно использовать оба домена одновременно
- Google индексирует оба домена (но лучше выбрать один основной)
- После перехода на новый домен позиции могут временно упасть (нормально)

---

**Готово!** После выполнения всех шагов сайт будет доступен по новому домену. 🎉
